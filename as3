

=================================
無條件捨棄
=================================

	var temp:Number =Math.random()*8; 
	temp = Math.floor(temp*100)/100;
	trace(temp);
	trace(temp.toFixed(2));
	
=================================


	var temp:Number =Math.random()*8; 
	temp = Math.floor(temp*100)/100;
	trace(temp);
	trace(temp.toFixed(2));

=================================
